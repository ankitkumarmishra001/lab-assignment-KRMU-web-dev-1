# Prompt Quizzer – JavaScript Quiz Game

This project is a simple quiz game made using JavaScript.  
The quiz runs completely inside the **browser console**, and it uses `prompt()` to take answers from the user and `alert()` to show feedback.

## 📌 Features
- Predefined list of quiz questions stored in an array.
- Uses a `for` loop to go through each question.
- Converts user input using `toLowerCase()` and `trim()` for accuracy.
- Gives instant feedback after every question.
- Tracks the total score and shows it at the end.
- No HTML or CSS required.

## 📁 Files
- `quiz.js` – contains the complete JavaScript code of the quiz.

## 🚀 How to Run
1. Open any web browser (Chrome recommended).
2. Press **F12** to open **Developer Tools**.
3. Go to the **Console** tab.
4. Copy all the code from `quiz.js`.
5. Paste it into the console and press **Enter**.
6. The quiz will start with prompt messages.

## 📝 Example Questions Used
- Capital of India  
- Red Planet  
- Father of Computer  
- Meaning of HTML  
- Largest ocean

## 📌 Requirements Completed
✔ Array of questions  
✔ Function to run quiz  
✔ Looping through questions  
✔ Input handling (tolowercase + trim)  
✔ Immediate feedback  
✔ Score tracking  
✔ Final score display  

## 👤 Author
Made by a B.Tech 1st year CSE student for Web Development Lab Assignment.
